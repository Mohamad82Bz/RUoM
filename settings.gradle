pluginManagement {
    repositories {
        maven {
            url = "https://repo.screamingsandals.org/releases"
        }

        gradlePluginPortal()
    }
}

rootProject.name = 'RUoM'

include 'core'
include 'common'
include 'bukkit'
include 'velocity'
include 'bungeecord'
