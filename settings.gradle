pluginManagement {
    repositories {
        maven {
            url = "https://repo.screamingsandals.org/public/"
        }

        gradlePluginPortal()
    }
}

rootProject.name = 'ruom'

include 'core'
include 'bukkit'
include 'velocity'
include 'bungeecord'
